apiVersion: v2
name: unrelated-mcp-gateway
description: MCP Gateway runtime/admin Helm chart.
type: application
version: 0.2.5
appVersion: "0.13.1"
dependencies:
  - name: unrelated-mcp-postgres
    version: 0.2.0
    repository: file://../unrelated-mcp-postgres
    alias: postgres
    condition: postgres.enabled

{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "unrelated-mcp-gateway.serviceAccountName" . }}
  labels:
{{ include "unrelated-mcp-gateway.labels" . | indent 4 }}
{{- end }}

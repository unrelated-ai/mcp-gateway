{{- if .Values.auth.adminToken.value }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "unrelated-mcp-gateway-ui.inlineSecretName" . }}
  labels:
{{ include "unrelated-mcp-gateway-ui.labels" . | indent 4 }}
type: Opaque
stringData:
  GATEWAY_ADMIN_TOKEN: {{ .Values.auth.adminToken.value | quote }}
{{- end }}
